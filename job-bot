# Initialize git
git init
git add .
git commit -m "Initial commit: JobBot automated job application bot"

# Connect to your GitHub repo (replace YOUR-USERNAME with your actual username)
git remote add origin https://github.com/jasper-john-paitan/jobbot.git

# Push
git branch -M main
git push -u origin main
